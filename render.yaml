services:
  - type: web
    name: ethan-dapp-server
    runtime: bun
    buildCommand: bun install && bun run build
    startCommand: bun run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET_KEY
        sync: false
